#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <time.h>
#include <stdlib.h>
#include <string.h>

int gcd(int, int);

void juggling(char*, int, int);
void trivialSolution(char* ary, int length, int move);
void Swap(char*, int , int , int );
void Block_Swap(char arr[], int , int );
void reverse(char*, int , int );
void Reverse(char*, int , int );